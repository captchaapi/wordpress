# Files and directories excluded from the distributed plugin build
# (e.g. `wp dist-archive`). Keeps dev-only artifacts out of the WP.org zip.
.git
.github
.gitignore
.idea
.wordpress-org
.distignore
README.md
languages/.build
.DS_Store
